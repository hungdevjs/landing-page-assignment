import { Box } from '@mui/material';
import Layout from '../../components/Layout';

const Register = () => {
  return (
    <Layout>
      <Box>Register</Box>
    </Layout>
  );
};

export default Register;
